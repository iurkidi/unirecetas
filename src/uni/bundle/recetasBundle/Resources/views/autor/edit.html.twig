{% extends 'uniRecetasBundle::layout.html.twig' %}

{% block central -%}
    <h1>Editar autor</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('autor') }}">
            Volver atras
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
